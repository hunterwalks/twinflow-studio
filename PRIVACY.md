# 隐私说明（TwinFlow Studio）

最后更新：v0.7.0

本文说明 TwinFlow Studio 如何处理你的数据。核心结论：**本应用为 local-first（本地优先），你的数据不会离开你的浏览器。**

## 1. 数据在哪里处理

- CSV / XLSX 文件在**你的浏览器内**解析，不会上传到任何业务后端或第三方服务器。
- 字段映射、确定性校验、关系图布局、质量评分、规则建议与报告导出（HTML / JSON）全部在浏览器本地计算。
- 没有任何接口会把你的表格内容发送到外部。你可以用浏览器开发者工具的「网络（Network）」面板自行验证：导入、校验、导出过程中**没有任何外部请求**。

## 2. 数据存储位置

- 当前项目数据（Demo 或导入结果）保存在浏览器的 **`localStorage`**（键 `twinflow-project-v1`）。
- 仅保存在你当前使用的浏览器中，不会同步到云端、不会跨设备共享。
- 关闭或刷新页面后，下次打开会自动从本地恢复。

## 3. 如何清空本地数据

- 方式一：进入「关系图」页面，点击「清空项目」。
- 方式二：在浏览器设置中清除本站（域名）的站点数据 / Cookie 与本地存储。

## 4. localStorage 不可用的情况

部分浏览器在**隐私模式**或**阻止站点数据**时不允许写入 `localStorage`：

- 应用顶部会出现黄色提示，说明数据将无法在刷新后恢复及其恢复方式。
- 应用功能不受影响，只是本次会话的数据不会被持久化。
- 恢复方式：关闭隐私模式、允许本站本地存储，或在其他支持本地存储的浏览器中打开。

## 5. 我们不会收集什么

- 不收集你的表格内容、文件名、对象名称等任何业务数据。
- 不要求登录、不绑定账号、不部署任何遥测或埋点。
- 仓库与发布产物仅包含合成数据，不含有任何真实客户、个人或敏感信息。

## 6. 静态托管说明

- 本项目可部署到 GitHub Pages 等免费静态托管。静态托管只负责**分发前端代码**，**不接收、不存储你处理的数据**。
- 你在本机处理的数据始终留在你的浏览器中。

如有疑问，请以仓库 README 与本文为准；本文与代码行为不一致时，以实际代码（local-first、无后端）为准。
